define(['app/hero'], function(Hero) {
  describe("Hero", function() {
    var hero;

    describe(".draw", function() {
      beforeEach(function() {
        hero = new Hero({});
      });
    });
  });
});